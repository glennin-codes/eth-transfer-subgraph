type Transfer @entity(immutable: true) {
  id: Bytes!
  from: Bytes!
  to: Bytes!
  value: BigInt!       # Amount in wei (ETH) or smallest unit (USDC)
  asset: String!       # "ETH" or "USDC"
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}